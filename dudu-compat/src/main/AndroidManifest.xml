<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.example.receiverapp.permission.DUDU_BRIDGE" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_dudu_notification"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_dudu_notification"
        android:supportsRtl="true"
        android:theme="@style/Theme.DuduCompat">

        <activity
            android:name=".DuduCompatActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".DuduBridgeReceiver"
            android:exported="true"
            android:permission="com.example.receiverapp.permission.DUDU_BRIDGE">
            <intent-filter>
                <action android:name="com.example.duducompat.POST" />
                <action android:name="com.example.duducompat.CANCEL" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".DuduActionReceiver"
            android:exported="false" />
    </application>
</manifest>
